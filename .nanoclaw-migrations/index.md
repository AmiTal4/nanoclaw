# NanoClaw Migration Guide

Generated: 2026-07-21
Base: `87a2d3af8dd826101d6dc1a7d3958e39486fa626`
HEAD at generation: `73b7f36d46d45811d9da9d2337b883ee56efcee6`
Upstream: `641963c1` (NanoClaw `2.1.53`)
Tier: 3 (complex)

## Objective

Rebuild this customized fork on clean upstream without losing behavior. The
pre-migration source remains reachable through
`pre-migrate-73b7f36d-20260721-225058` and
`backup/pre-migrate-73b7f36d-20260721-225058`. The uncommitted Codex provider
work is separately preserved in the stash named
`pre-migration Codex provider work 2026-07-21`.

## Migration plan

1. Start from `upstream/main` in an isolated worktree.
2. Apply the current upstream versions of installed channel/provider/tool
   skills. Do not copy their old implementations wholesale.
3. Port WhatsApp behavior from [whatsapp.md](whatsapp.md), retaining a focused
   regression test for every behavior.
4. Port host behavior from [host.md](host.md), adapting it to upstream's guard,
   explicit-destination, task, channel-default, and memory contracts.
5. Port container and operational integrations from
   [integrations.md](integrations.md).
6. Run staged host, runner, channel, and container validation.
7. Review required data migrations, task contracts, channel skill refresh, and
   session continuity. `/migrate-memory` is intentionally deferred because the
   Codex provider switch is not part of this upgrade.
8. Commit the candidate in the worktree. Do not move `main` until all required
   validation passes.

## Applied skills and integrations

- `/add-codex` (the in-progress local version is stashed; apply it only after
  the committed fork migration is stable, then reconcile it with upstream
  shared memory)
- `/add-telegram`
- `/add-whatsapp`
- `/add-whatsapp-cloud`
- `/add-slack`
- `/add-bitwarden-secrets`
- `/add-monday-tool`

The channel skills must be applied from their current upstream recipes because
Chat SDK adapters are now pinned with `chat@4.29.0`, channel formatting skills
moved to the `channels` branch, and channel defaults are now declarative.

## Required customizations

- Native WhatsApp polls/events, poll-vote reception/decryption, approval polls,
  contact cards, replies/quoted messages, media staging, group mentions, and WA
  Web version fallback.
- Cross-session delivery mirroring and unforgeable origin metadata.
- Multi-instance destination-grant resolution and thread-targeted sends.
- Per-agent-group activity journal and agent-to-agent provenance.
- Pull-based channel history and its container tool.
- Provider-limit loop containment and repeat suppression.
- Multi-instance Slack identities and teammate automation.
- Bitwarden Secrets Manager, monday.com, AWS STS, GitHub App, Telegram, and
  webhook logging customizations.
- Public-agent disabled-tool enforcement and local/private WebFetch blocking.

## Skill interactions and upstream risks

- WhatsApp is a native Baileys adapter; reapply `/add-whatsapp` first and then
  layer the custom behavior. Do not restore the old adapter as a single file.
- Slack/WhatsApp formatting skills must come from the refreshed channel skills.
- Delivery customizations must re-enter upstream's centralized `guard()` seam;
  no replay may use the removed forgeable `approved: true` mechanism.
- All sends must retain upstream's required explicit `to` destination.
- Legacy scheduling MCP changes must not be restored. Translate only the
  Unicode-safe truncation behavior to `ncl tasks` formatting if still needed.
- Memory is now provider-agnostic. Do not restore old provider-specific memory
  layout while reconciling the stashed Codex provider.
- Migration numbers collide: the fork has local migrations 019-021 while
  upstream already owns migration 019. Recreate them at the next free upstream
  numbers; never copy the old numbers.
- `package.json` and lockfiles must be regenerated by current skill installs and
  package-manager commands, not copied from the old fork.

## Validation gate

The migration is incomplete until all of these pass:

- `pnpm run build`
- `pnpm test`
- `pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit`
- `cd container/agent-runner && bun test`
- `./container/build.sh`
- Targeted WhatsApp tests listed in `whatsapp.md`
- Targeted delivery, history, journal, Slack, and provider-limit tests listed in
  `host.md` and `integrations.md`

## Completion record

Completed on 2026-07-21. The candidate was rebuilt on upstream `641963c1` and
all required custom behavior above was replayed. The old v1 group-sync path and
the separate container-config bootstrap commit were intentionally superseded by
the v2 entity model and current group filesystem/config initialization.

Validation results:

- Host TypeScript build passed.
- Full host suite passed: 104 files, 1,243 tests.
- Full Bun agent-runner suite passed in the built container: 184 passed, 1
  skipped, 0 failed.
- Focused WhatsApp suite passed: 34 tests.
- Candidate container image built successfully with frozen agent-runner lockfile.
- Live `Edna Public` restrictions were verified read-only in `data/v2.db`:
  disabled tools remain configured and local/private WebFetch blocking is on.

The Codex-provider stash remains stored and was not restored, per operator
instruction. After promotion, the single live group still configured for the
deferred Codex provider (`Edna`) was returned to the built-in Claude provider
and its Codex-specific model override was cleared. Shared memory was preserved;
`/migrate-memory` was neither needed nor run. Group workspaces and credentials
were not rewritten during candidate construction.
